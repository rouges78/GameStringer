$file = "c:\dev\GameStringer\docs\USER_GUIDE_RU.md"
$content = [System.IO.File]::ReadAllText($file, [System.Text.Encoding]::UTF8)

# Find insertion point: the --- separator before ## What's New in v1.4.0
$whatsNew = $content.IndexOf("## What's New in v1.4.0")
if ($whatsNew -lt 0) { Write-Host "ERROR: What's New not found"; exit 1 }

# Find the --- separator before it (search backwards)
$sepIdx = $content.LastIndexOf("---", $whatsNew)
if ($sepIdx -lt 0) { Write-Host "ERROR: separator not found"; exit 1 }

# Detect line ending
$crlf = $content.Contains("`r`n")
$nl = if ($crlf) { "`r`n" } else { "`n" }

$sections = @"
---${nl}${nl}## Глоссарий${nl}${nl}Глоссарий управляет пользовательскими словарями терминологии для каждой игры, обеспечивая согласованность переводов.${nl}${nl}### Функции${nl}${nl}- **Уровни терминов**:${nl}  - 🔴 **Locked** — термин всегда переводится идентично (имена собственные, заклинания, места)${nl}  - 🟡 **Synced** — согласованный перевод, адаптируемый к контексту${nl}  - 🟢 **Flexible** — свободный перевод${nl}- **Категории**: персонаж, место, предмет, навык, квест, UI, система, лор, существо, фракция${nl}- **Автоизвлечение**: анализ AI для предложения терминов${nl}- **Проверка согласованности**: проверяет единообразный перевод во всех файлах${nl}- **Импорт/Экспорт**: CSV и JSON для обмена глоссариями между играми${nl}${nl}### Как использовать${nl}${nl}1. Перейдите в **Расширенные инструменты → Глоссарий**${nl}2. Выберите игру из списка${nl}3. Добавьте термины вручную или используйте **«Извлечь термины»** для предложений AI${nl}4. Установите уровень для каждого термина${nl}5. Глоссарий применяется автоматически при переводах${nl}
"@

Write-Host "CRLF: $crlf, sepIdx: $sepIdx, whatsNewIdx: $whatsNew"
Write-Host "Content around sep: '$($content.Substring([Math]::Max(0,$sepIdx-5), 20))'"

# Build: everything up to (not including) the --- separator + new sections + original --- + rest
$before = $content.Substring(0, $sepIdx)
$after = $content.Substring($sepIdx)

# The new content to insert (all 8 sections) before the existing --- What's New
$newSections = "---" + $nl + $nl + "## Глоссарий" + $nl + $nl + `
    "Глоссарий управляет пользовательскими словарями терминологии для каждой игры, обеспечивая согласованность переводов." + $nl + $nl + `
    "### Функции" + $nl + $nl + `
    "- **Уровни терминов**:" + $nl + `
    "  - 🔴 **Locked** — термин всегда переводится идентично (имена собственные, заклинания, места)" + $nl + `
    "  - 🟡 **Synced** — согласованный перевод, адаптируемый к контексту" + $nl + `
    "  - 🟢 **Flexible** — свободный перевод" + $nl + `
    "- **Категории**: персонаж, место, предмет, навык, квест, UI, система, лор, существо, фракция" + $nl + `
    "- **Автоизвлечение**: анализ AI для предложения терминов" + $nl + `
    "- **Проверка согласованности**: проверяет единообразный перевод во всех файлах" + $nl + `
    "- **Импорт/Экспорт**: CSV и JSON для обмена глоссариями между играми" + $nl + $nl + `
    "### Как использовать" + $nl + $nl + `
    "1. Перейдите в **Расширенные инструменты → Глоссарий**" + $nl + `
    "2. Выберите игру из списка" + $nl + `
    "3. Добавьте термины вручную или используйте **«Извлечь термины»** для предложений AI" + $nl + `
    "4. Установите уровень для каждого термина" + $nl + `
    "5. Глоссарий применяется автоматически при переводах" + $nl + $nl + `
    "---" + $nl + $nl + `
    "## Context Harvester" + $nl + $nl + `
    "Анализирует текстовые строки, классифицирует их и обогащает контекстом перед переводом AI, улучшая качество." + $nl + $nl + `
    "### Функции" + $nl + $nl + `
    "- **Автоматическая классификация**: определяет тип экрана (меню, диалог, нарратив, обучение, система)" + $nl + `
    "- **Распознавание говорящего**: определяет кто говорит и тип тона (формальный, разговорный, агрессивный)" + $nl + `
    "- **Контекстные метаданные**: каждая строка получает жанр игры, тип контента и тон" + $nl + `
    "- **Сохранение harvest**: извлечённые контексты сохраняются и используются повторно" + $nl + `
    "- **Пакетная обработка**: анализирует целые файлы за одну операцию" + $nl + $nl + `
    "### Как использовать" + $nl + $nl + `
    "1. Перейдите в **Расширенные инструменты → Context Harvester**" + $nl + `
    "2. Вставьте строки или загрузите файл" + $nl + `
    "3. Нажмите **«Анализировать»** для классификации каждой строки" + $nl + `
    "4. Скачайте результат JSON для использования как входных данных для переводов AI" + $nl + $nl + `
    "---" + $nl + $nl + `
    "## Память переводов" + $nl + $nl + `
    "Постоянная база данных всех выполненных переводов с автоматическим повторным использованием." + $nl + $nl + `
    "### Функции" + $nl + $nl + `
    "- **Автоматическое повторное использование**: уже переведённые строки предлагаются без нового вызова AI" + $nl + `
    "- **Поиск**: по оригинальному тексту, переводу или названию игры" + $nl + `
    "- **Фильтр по игре**: отображает только переводы конкретного названия" + $nl + `
    "- **Статистика**: общие единицы, распределение по игре, дата последнего изменения" + $nl + `
    "- **Экспорт**: JSON, CSV, TMX для других CAT-инструментов" + $nl + `
    "- **Импорт**: импортирует существующие переводы из TMX или CSV" + $nl + $nl + `
    "### Как использовать" + $nl + $nl + `
    "1. Перейдите в **Расширенные инструменты → Память переводов**" + $nl + `
    "2. Найдите предыдущие переводы с помощью панели поиска" + $nl + `
    "3. Редактируйте или удаляйте отдельные единицы по необходимости" + $nl + `
    "4. Память автоматически консультируется при переводах AI" + $nl + $nl + `
    "---" + $nl + $nl + `
    "## OCR-переводчик" + $nl + $nl + `
    "Захватывает текст из любого игрового окна или снимка экрана в реальном времени и мгновенно переводит его." + $nl + $nl + `
    "### Функции" + $nl + $nl + `
    "- **Захват в реальном времени**: анализирует экран с настраиваемыми интервалами" + $nl + `
    "- **Исходные языки**: японский, английский, упрощённый китайский, корейский" + $nl + `
    "- **Выбор окна**: указывает непосредственно на игровое окно" + $nl + `
    "- **Выбор региона**: определяет конкретную область экрана для анализа" + $nl + `
    "- **Достоверность**: показывает уровень надёжности для каждого обнаруженного текста" + $nl + `
    "- **Глобальная горячая клавиша**: включает/выключает захват сочетанием клавиш" + $nl + `
    "- **Кэш переводов**: повторно использует предыдущие переводы для идентичных строк" + $nl + $nl + `
    "### Как использовать" + $nl + $nl + `
    "1. Перейдите в **OCR-переводчик** из боковой панели" + $nl + `
    "2. Выберите исходный язык игры" + $nl + `
    "3. Нажмите **«Выбрать окно»** и выберите игровое окно" + $nl + `
    "4. *(Необязательно)* Установите конкретный регион через **«Выбрать регион»**" + $nl + `
    "5. Нажмите **«Пуск»** для начала автоматического захвата и перевода" + $nl + $nl + `
    "---" + $nl + $nl + `
    "## AI-проверка" + $nl + $nl + `
    "Автоматическая проверка качества переводов с обнаружением ошибок и предложениями исправлений." + $nl + $nl + `
    "### Функции" + $nl + $nl + `
    "- **Единичный режим**: проверка одной пары оригинал/перевод" + $nl + `
    "- **Пакетный режим**: массовая проверка в формате " + '`' + "оригинал|перевод" + '`' + " на строку" + $nl + `
    "- **Категории проблем**: точность, беглость, терминология, тон, структура" + $nl + `
    "- **Уровни серьёзности**: критический, предупреждение, информация" + $nl + `
    "- **Авто-исправление**: автоматическое исправление незначительных проблем" + $nl + `
    "- **Статистика**: глобальная оценка 0–100 для каждого пакета" + $nl + $nl + `
    "### Как использовать" + $nl + $nl + `
    "1. Перейдите в **Расширенные инструменты → AI-проверка**" + $nl + `
    "2. Выберите **Единичный** или **Пакетный**" + $nl + `
    "3. Вставьте оригинальный текст и перевод" + $nl + `
    "4. Нажмите **«Проверить»** для получения отчёта" + $nl + `
    "5. Используйте **«Авто-исправление»** для применения предложенных исправлений" + $nl + $nl + `
    "---" + $nl + $nl + `
    "## AI-пайплайн" + $nl + $nl + `
    "Автоматизированный 6-шаговый рабочий процесс для получения переводов максимального качества одним кликом." + $nl + $nl + `
    "### Шаги пайплайна" + $nl + $nl + `
    "1. **Harvest** — извлекает и классифицирует контекст" + $nl + `
    "2. **Translate** — переводит с настроенным провайдером AI" + $nl + `
    "3. **QA Check** — автоматическая проверка качества" + $nl + `
    "4. **Auto-Fix** — исправляет найденные проблемы" + $nl + `
    "5. **Review** — финальная проверка AI" + $nl + `
    "6. **Score** — вычисляет итоговую оценку 0–100" + $nl + $nl + `
    "### Доступные пресеты" + $nl + $nl + `
    "- **Quick** — основные шаги (Translate + QA Check)" + $nl + `
    "- **Max Quality** — все 6 шагов по порядку" + $nl + $nl + `
    "### Как использовать" + $nl + $nl + `
    "1. Перейдите в **Расширенные инструменты → AI-пайплайн**" + $nl + `
    "2. Вставьте строки для перевода" + $nl + `
    "3. Выберите пресет или настройте шаги вручную" + $nl + `
    "4. Нажмите **«Запустить пайплайн»**" + $nl + `
    "5. Скачайте итоговый отчёт с оценками для каждой строки" + $nl + $nl + `
    "---" + $nl + $nl + `
    "## Переводчик эмоций" + $nl + $nl + `
    "Перевод, анализирующий и сохраняющий эмоции в оригинальном диалоге." + $nl + $nl + `
    "### Функции" + $nl + $nl + `
    "- **Анализ эмоций**: определяет преобладающую эмоцию (гнев, грусть, страх, радость, нейтральная, удивление, отвращение)" + $nl + `
    "- **Интенсивность**: измеряет уровень эмоциональной интенсивности (0–100)" + $nl + `
    "- **Сохранение тона**: направляет AI на сохранение того же эмоционального воздействия" + $nl + `
    "- **EmotionBadge**: визуальная метка для каждой строки с эмоцией и интенсивностью" + $nl + `
    "- **Пакетная статистика**: распределение эмоций по всему файлу" + $nl + $nl + `
    "### Как использовать" + $nl + $nl + `
    "1. Перейдите в **Расширенные инструменты → Переводчик эмоций**" + $nl + `
    "2. Вставьте текст для перевода" + $nl + `
    "3. Выберите целевой язык" + $nl + `
    "4. Нажмите **«Анализировать и перевести»**" + $nl + `
    "5. Результат показывает перевод с идентифицированными эмоциями для каждой строки" + $nl + $nl + `
    "---" + $nl + $nl + `
    "## Культурная адаптация" + $nl + $nl + `
    "Анализирует переведённый текст для выявления культурно проблематичных элементов и предлагает адаптации." + $nl + $nl + `
    "### Функции" + $nl + $nl + `
    "- **Поддерживаемые культуры**: IT, EN, DE, FR, ES, JA, KO, ZH, PT, RU" + $nl + `
    "- **Анализируемые категории**: идиоматические выражения, культурные отсылки, меры/валюты, символические цвета, формулы вежливости, юмор" + $nl + `
    "- **Конкретные предложения**: для каждого элемента предлагает альтернативу, подходящую целевой культуре" + $nl + `
    "- **Оценка адаптации**: процент текста, требующего пересмотра" + $nl + $nl + `
    "### Как использовать" + $nl + $nl + `
    "1. Перейдите в **Расширенные инструменты → Культурная адаптация**" + $nl + `
    "2. Вставьте переведённый текст" + $nl + `
    "3. Выберите исходную и целевую культуру" + $nl + `
    "4. Нажмите **«Анализировать»**" + $nl + `
    "5. Примените предложения перед финальной публикацией" + $nl + $nl

$newContent = $before + $newSections + $after
[System.IO.File]::WriteAllText($file, $newContent, [System.Text.Encoding]::UTF8)
Write-Host "Done. Inserted at position $sepIdx"
